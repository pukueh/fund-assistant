# 基金估值助手配置

# ============ 环境配置 ============
# 开发环境: development (默认)
# 生产环境: production (启用严格安全检查、禁止mock数据源回退)
ENVIRONMENT=development

# ============ LLM配置（复用HelloAgents的配置）============
LLM_MODEL_ID=your-model-name
LLM_API_KEY=your-api-key
LLM_BASE_URL=your-api-base-url

# ============ 安全配置 ============
# ⚠️ 生产环境必须配置！否则服务重启后所有登录 Token 失效
# 使用命令生成: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your-secure-jwt-secret-key

# ============ 服务配置 ============
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
DEBUG=false

# ============ 行情数据 API 配置 ============
# 选择数据源: auto (自动回退) / eastmoney / akshare / tushare / mock
# 生产环境推荐使用 auto 或指定具体数据源，禁止使用 mock
MARKET_DATA_SOURCE=auto

# TuShare 配置 (推荐，需注册获取token)
# 官网: https://tushare.pro
# 注册后在个人中心获取token
TUSHARE_TOKEN=your_tushare_token_here

# AKShare 配置 (免费，无需token，但不如TuShare稳定)
# 文档: https://akshare.akfamily.xyz

# 东方财富 配置 (通过爬虫，无需token)
# 用于基金净值、持仓等数据

# ============ 黄金/商品行情 API ============
# 新浪财经黄金接口 (免费，无需token)
# 伦敦金XAU: https://hq.sinajs.cn/list=hf_GLD
# 黄金9999:  https://hq.sinajs.cn/list=hf_AUTD

# 金十数据 (付费API，实时行情)
# JINSHI_API_KEY=your_jinshi_api_key

# ============ 持仓数据路径 ============
PORTFOLIO_PATH=./data/portfolio.json

# ============ 使用说明 ============
# 1. 复制此文件为 .env
#    cp .env.example .env
#
# 2. 填写配置:
#    - 如需真实行情: 注册 TuShare (https://tushare.pro) 获取 token
#    - 如用模拟数据: 保持 MARKET_DATA_SOURCE=mock 即可
#
# 3. TuShare 示例代码 (server中使用):
#    import tushare as ts
#    ts.set_token(os.getenv('TUSHARE_TOKEN'))
#    pro = ts.pro_api()
#    df = pro.index_daily(ts_code='000001.SH')  # 上证指数
#
# 4. AKShare 示例代码:
#    import akshare as ak
#    df = ak.stock_zh_index_spot()  # A股指数实时行情
#    df = ak.futures_zh_spot()      # 期货/商品实时行情
